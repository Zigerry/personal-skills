interface:
  display_name: "Session Analysis"
  short_description: "Audit sessions, agents, timing, context, and compaction"
  default_prompt: "Use $session-analysis to audit the previous coding session, including agent timing, context usage, compactions, failures, and unfinished work."
